# プライバシーポリシー - Rinkaku

最終更新日: 2026年8月15日

## はじめに

「Rinkaku」（以下「本拡張機能」）は、ユーザーのプライバシーを尊重し、個人情報の保護に努めます。本プライバシーポリシーは、本拡張機能におけるデータの取り扱いについて説明します。

## 取得するデータ

本拡張機能は、購入とライセンス認証のために限り、次の情報を取得します。

- **メールアドレス**: 購入の照合、6 桁の確認コードの送信、ライセンス認証、お問い合わせ対応に使用します。
- **購入情報**: 購入 ID、購入日時、購入した商品、返金・失効状態を、ライセンスの発行と有効性確認に使用します。
- **6 桁の確認コード**: 購入時のメールアドレスを受信できることの確認に使用します。コードを平文のまま保存せず、ハッシュ化した値だけを有効期限つきで保持します。

Kagayoi のサーバーは、閲覧履歴、閲覧中のページ URL、視聴した動画の内容、検索語、ページに入力した内容、端末を識別する情報を受信・記録しません。本拡張機能が端末内で扱う動画フレームと、利用者が明示的に保存したページ単位設定については、以下で説明します。

## ローカルに保存するデータ

本拡張機能は、以下の設定データをユーザーの端末内の `chrome.storage.local` と `chrome.storage.sync` に保存します。

- 処理の有効・無効
- モード（アニメ / 実写）
- 強度（低 / 中 / 高）
- ページ単位で記憶した場合の対象識別子（YouTube のチャンネル ID、X のアカウント名など）と、その設定
- ライセンス情報（試用開始日時、メール認証後に発行され画面には表示されない内部credential、最後にオンライン確認が成功した日時）。認証時に入力したメールアドレスと確認コードは保存しません。

あわせて、体験期間の開始日時のみを `sekisho.kagayoi.com` の Cookie（`rinkaku_trial`）としても端末内に保存します。これは本拡張機能をアンインストールしても体験期間の残りが分かるようにするためのもので、**この Cookie が外部へ送信されることはありません**（本拡張機能が通信に使うアドレスには付かないパスに限定しています）。保存されるのは日時の数値のみで、個人を特定できる情報は含みません。

`chrome.storage.sync` は Chrome にログインしている場合、Google のアカウント同期機能によって同じアカウントの他の端末へ同期されます。これは Chrome 自体の機能であり、本拡張機能の運営者がこれらのデータを受け取ることはありません。同期を望まない場合は、Chrome の同期設定で「拡張機能」を除外してください。

## 動画の処理について

動画の映像はすべてユーザーの端末上の GPU で処理されます。**映像データが端末の外へ送信されることはありません。**

## ネットワーク通信

画質処理そのものに外部通信はありません。処理に使うプログラムとニューラルネットの重みはすべて拡張機能のパッケージに同梱されており、実行時に取得するものはありません。

外部と通信するのは、以下のライセンス関連の場面だけです。通信先は当方が運営する `https://sekisho.kagayoi.com` です。

| 場面 | 送信する内容 |
|---|---|
| ライセンスの有効性確認（1 日 1 回程度） | 購入 ID（Stripe の決済 ID） |
| 購入ページを開いたとき | なし（購入ページへ移動するだけ） |
| 確認コードの送信を依頼したとき | 購入時に入力されたメールアドレス |
| 確認コードを入力して認証するとき | メールアドレスと確認コード |

いずれの通信でも、閲覧中のページの URL、動画の内容、拡張機能の設定は送信しません。試用期間中は、有効性確認の通信も発生しません。

決済は Stripe, Inc. が処理し、当方はカード番号を受け取りません。確認コードのメール送信には Resend, Inc. を使用します。メールアドレスはライセンス認証、購入の照合、お問い合わせ対応にのみ使用します。

これ以外の外部通信（解析、統計、広告）は一切行いません。

## データの共有

取得したデータを販売したり、広告、信用力の判定、または本拡張機能の提供と無関係な目的に使用したりすることはありません。

決済処理のため Stripe, Inc.、確認コードのメール送信のため Resend, Inc. を利用しており、それぞれの目的に必要な範囲で情報が渡ります。法令に基づく場合を除き、これ以外の第三者へ個人情報を提供しません。

## 保存期間と削除

メールアドレスと購入情報は、ライセンスの有効性確認、返金対応および法令上必要な期間に限って保存します。確認コードは有効期限つきのハッシュとして保存し、使用後または有効期限後は認証に利用できなくなります。

保有する個人情報の開示、訂正または削除を希望する場合は、`contact@kagayoi.com` へご連絡ください。購入記録を削除した場合、その購入に基づく再認証や別端末での利用ができなくなる場合があります。

## Chrome Web Store の限定利用要件

本拡張機能が取り扱うユーザーデータは、画質処理機能の提供、購入者の認証、ライセンスの有効性確認、セキュリティ維持およびお問い合わせ対応のためだけに使用します。広告、プロファイリング、信用力の判定、またはこれらと無関係な目的には使用しません。映像、閲覧履歴、ページ内容を人が閲覧できる仕組みは設けていません。

## 権限の使用目的

- **storage**: 上記の設定とライセンス情報をローカルに保存・復元するために使用します。
- **alarms**: ライセンスの有効性を 1 日 1 回確認するために使用します。
- **cookies**: 体験期間の開始日時を保存・読み取りするためだけに使用します。閲覧中のサイトの Cookie の読み取り・変更・送信は行いません。
- **ホストへのアクセス（すべてのサイト）**: 動画要素を見つけて処理結果を重ねて表示するために使用します。ページの内容の読み取り・送信は行いません。ライセンスの確認先 `https://sekisho.kagayoi.com` への通信もこの権限に含まれます。

## お問い合わせ

本プライバシーポリシーに関するご質問は、[Rinkaku の公開サポート窓口](https://github.com/1llum1n4t1s/Rinkaku-Support/issues) または Chrome Web Store のサポートページよりお問い合わせください。

## 変更について

本プライバシーポリシーは予告なく変更される場合があります。変更があった場合は、本ページを更新します。
